---
import Layout from "../layouts/Layout.astro"
import LoginSection from "@/sections/LoginSection.astro"

const cookie = Astro.cookies.get("access_token")?.value
if (cookie) return Astro.redirect('/home')
---

<Layout title="VoxPet-Web">
	<LoginSection />
</Layout>
